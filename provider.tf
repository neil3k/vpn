# Configure the AWS Provider
# This sets up the connection to AWS services in the specified region
provider "aws" {
  region = "eu-west-2"    # London region - change this to your preferred region
}